# yaml-language-server: $schema=https://fortify.github.io/fcli/schemas/action/fcli-action-schema-dev-2.x.json

author: Fortify
usage:
  header: Test shared sessions
  description: |
    TODO

config:
  output: immediate
  session.from-env.output: suppress
  
steps:
  - var.set:
      ci.x: X value
      ci.y: Y value
  - out.write:
      stdout: ${ci}